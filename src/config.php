<?php
// Define the base URL for the application.
// This is used for generating absolute links in emails, especially when run from CLI.
// Adjust this if your application is served from a different URL or port.
if (!defined('APP_URL')) {
    define('APP_URL', 'http://localhost:8000');
}
